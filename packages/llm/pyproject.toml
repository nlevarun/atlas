[project]
name = "atlas-llm"
version = "0.1.0"
description = "LLM provider abstraction for Atlas"
requires-python = ">=3.12"
dependencies = [
    "redis>=5.0.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
