[project]
name = "atlas-orchestrator"
version = "0.1.0"
description = "Research orchestration for Atlas"
requires-python = ">=3.12"
dependencies = [
    "atlas-schemas",
    "atlas-agents",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
